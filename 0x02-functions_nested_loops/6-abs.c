#include "main.h"

/**
 * _abs - computes the absolute value of an intrger
 * @b: The integer to be computed
 * Return: The absolute value
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
}
	else
{
		return (-b);
}
